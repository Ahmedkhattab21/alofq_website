document.addEventListener('DOMContentLoaded',()=>{const links=document.querySelectorAll('.legal-index a');const sections=document.querySelectorAll('.legal-section');if(links.length&&sections.length&&'IntersectionObserver'in window){const observer=new IntersectionObserver(entries=>entries.forEach(entry=>{if(entry.isIntersecting){links.forEach(link=>link.classList.toggle('active',link.getAttribute('href')===`#${entry.target.id}`))}}),{rootMargin:'-25% 0px -65%'});sections.forEach(section=>observer.observe(section))}const items=document.querySelectorAll('[data-faq-item]');items.forEach(item=>{const button=item.querySelector('button');const answer=item.querySelector('div');button.addEventListener('click',()=>{const open=button.getAttribute('aria-expanded')==='true';button.setAttribute('aria-expanded',String(!open));answer.hidden=open})});const search=document.querySelector('[data-faq-search]');const categories=document.querySelectorAll('[data-faq-category]');const empty=document.querySelector('[data-faq-empty]');let category='all';const filter=()=>{const term=(search?.value||'').trim().toLowerCase();let visible=0;items.forEach(item=>{const show=(category==='all'||item.dataset.category===category)&&item.textContent.toLowerCase().includes(term);item.classList.toggle('is-hidden',!show);if(show)visible++});if(empty)empty.hidden=visible>0};search?.addEventListener('input',filter);categories.forEach(button=>button.addEventListener('click',()=>{categories.forEach(b=>b.classList.remove('active'));button.classList.add('active');category=button.dataset.faqCategory;filter()}))});
